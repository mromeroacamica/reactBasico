import React from 'react';

const Footer = (props) => {
    return (
        <footer>
            <p>Todos los derechos reservados &copy; {props.anio}</p>
        </footer>

      );
}
 
export default Footer;
